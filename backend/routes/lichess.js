import express from "express";

const router = express.Router();

// Placeholder routes - will be implemented later
router.get("/", (req, res) => {
  res.json({ message: "Lichess integration routes - Coming soon" });
});

export default router;
